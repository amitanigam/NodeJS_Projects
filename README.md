Project banane ke steps :

1: npm init "npm install karta hai"

Nodemon install commnd
npm install nodemon --save-dev

Install express 
npm install express

npm init 
npm install nodemon --save-dev
npm install express --save
npm install body-parser --save

for tailwindsCss 
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init







// git pe already bani repo pe apna code upload karne 

git init
git remote add origin https://github.com/amitanigam/airbnb_nodejs.git
git add .
git commit -m "Initial commit: Airbnb Node.js + Tailwind project"
git branch -M main
git push -u origin main

https://github.com/amitanigam/NodeJS_Projects.git




